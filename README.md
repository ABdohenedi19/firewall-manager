# firewall-manager
Simple Python Firewall Manager is a lightweight tool built with Python that allows you to control the Windows Firewall easily.  It supports blocking applications from accessing the internet, deleting firewall rules, listing active rules, enabling firewall logging, and monitoring the firewall log file in real-time.
